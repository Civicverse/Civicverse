{
  "name": "civicverse-monorepo",
  "version": "1.0.0",
  "description": "Civicverse monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "bash ./scripts/dev.sh",
    "build": "npm run build --workspaces",
    "start:backend": "npm start --workspace=backend",
    "start:frontend": "npm run dev --workspace=frontend",
    "clean": "rm -rf node_modules backend/node_modules frontend/node_modules frontend/dist && rm -f package-lock.json backend/package-lock.json frontend/package-lock.json && echo 'Clean complete!'",
    "docker:clean": "docker-compose down --volumes && docker container prune -f && docker image prune -f && echo 'Docker cleanup complete!'"
  }
}
